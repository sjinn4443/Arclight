/**
 * @file Sample Test
 * @description A basic test file to ensure Jest is correctly configured and running within the project.
 */
describe("Sample Test", () => {
  test("should return true", () => {
    expect(true).toBe(true);
  });
});
